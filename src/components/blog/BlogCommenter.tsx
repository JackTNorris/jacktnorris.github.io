import React, { useState } from 'react'

export const BlogCommenter = () => {
    const [setComment, comment] = useState<string>('');
    return (
        <div className='w-full'>
            
        </div>
    );
}